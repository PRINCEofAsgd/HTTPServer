# 1. Linux 系统下基于 Reactor 模型的高并发服务器
## 1.1 性能测试
- 配置：四核虚拟机
- 少量连接并发请求：200k QPS
- 简单并发回显业务：40k/s
- 大量连接并发回显：650 连接 35k/s

# 2. 回显服务器
## 2.1 服务端程序
### 2.1.1 使用方法
`main_echo.cpp`
- 功能：回显客户端发送的请求。
- 说明：用于测试服务器的并发能力，客户端发送请求后，服务器会将请求内容回显给客户端。
- 示例：./httpserver 192.168.1.128 4819

### 2.1.2 层次结构
- `EchoServer`

## 2.2 客户端程序
### 2.2.1 使用方法
`client_input.cpp`
- 功能：向服务器发送逐个消息。
- 说明：用于测试服务器的实时通信能力，客户端输入exit命令后，服务器会关闭连接。
- 示例：./client_input 192.168.1.128 4819

`client_concurrent.cpp`
- 功能：非阻塞地循环向服务器发送并发消息。
- 说明：用于测试服务器的并发能力，客户端发送海量消息，将非阻塞地接收服务器的回显，能够测试服务器的最强并发处理能力。
- 缺陷：存在 bug，当循环发送的海量消息数量超过服务器的处理能力时，服务器会出现解析消息边界出错的现象。
- 示例：./client_concurrent 192.168.1.128 4819
- 并发脚本1示例：./conc.sh
- 并发脚本2示例：./concconn.sh

`client_test.cpp`
- 功能：阻塞地接收并行地与服务器交互测试消息。
- 说明：用于测试服务器的并发能力，客户端能精确稳定地发送和接收与服务器的交互消息，能够测试服务器的并发处理能力。
- 缺陷：由于阻塞处理，性能不如 client_concurrent.cpp，但稳定不会出现消息丢失和解析错误。
- 示例：./client_test 192.168.1.128 4819
- 并发脚本示例：./test.sh

# 3. HTTP Web 服务器
## 3.1 服务端程序
### 3.1.1 使用方法
`main_http.cpp`
- 功能：接收并处理 HTTP 请求报文并返回响应报文。
- 说明：用于处理客户端发送的 HTTP 请求报文，根据请求内容返回对应的响应报文。
- 示例：./HttpServerExample

`./static`
- 静态资源目录：用于存储服务器需要返回给客户端的静态资源。
- 示例：./static/index.html

## 3.2 客户端程序
### 3.2.1 使用方法
`main_client.cpp`
- 功能：向服务器发送 HTTP 请求报文。
- 说明：用于测试服务器的 HTTP 功能，客户端发送 HTTP 请求报文后，服务器会返回对应的响应报文。
- 示例：./main_client
